import { Component } from '@angular/core';

@Component({
  selector: 'app-footer',
  standalone: true, // This indicates that the component is standalone
  templateUrl: './footer.component.html', // Path to the footer template
  styleUrls: ['./footer.component.css'] // Path to the footer styles
})
export class FooterComponent {
  // You can add any logic related to the footer here, if necessary
}
